
class Test {

    public int randomFunction(int a, int b, boolean c) {
        a + b;
        return 1;
    }

    public static void main(String[] a) {
        int[] L;
        int i;
        int j;
        boolean b;
        Test q;

        3 + 2 / 4 - 3 * i + 2 + 4 * 2;
        1 - 2;
        i * i;
        i / j;
        b && b;
        i<j ;
        !b;


        L[i * j] = 1;

        /*// io.getArray()[i];

        j = L.length + 2;

        1 + this.randomFunction(L[1 + i], 2 * i + 3, b);

        new Test();

        new int[L.length];*/
    }
}