import io;

class Test {
    public static void main(String[] a) {
        int b;
        boolean flag;
        flag = true;
        b = 0;
        while (flag) {
            b = b + 1;
            if (b < 10) {
                flag = true;
            } else {
                flag = false;
            }
        }
        io.println(b);
    }
}
