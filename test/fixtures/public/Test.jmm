import Tclass;

class Test {
    int b;
    public static void main(String[] a) {
        int d;
        Tclass tclass;
        tclass = new Tclass();
        // b = this.func(true);
        d = tclass.func(a);
    }

    public int func(boolean flag) {
        int c;
        b = 1;
        c = b + 2;
        if (flag) {
            c = 1;
        } else {
            c = 2;
        }
        return c;
    }
}